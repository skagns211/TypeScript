import React from 'react';
import styled from 'styled-components';

const StyledBox = styled.span`
  border: 3px solid red;
`;

function Box() {
  return <StyledBox>hid</StyledBox>;
}

export default Box;
